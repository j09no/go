
{
  "version": 2,
  "buildCommand": "cd go && npm install && npm run build",
  "outputDirectory": "go/dist",
  "framework": null,
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
